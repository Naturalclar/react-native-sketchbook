import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs';
import {action} from '@storybook/addon-actions';
import {RoundButtonOutline} from './RoundButtonOutline';
import {Typography} from '../Typography';

<Meta
  title={'Buttons/RoundButtonOutline'}
  component={RoundButtonOutline}
  parameters={{layout: 'padded'}}
/>

# RoundButtonOutline

Example of Button Icon extended from Base Button

## Props

<ArgsTable of={RoundButtonOutline} />

export const Template = (args) => <RoundButtonOutline {...args} />;

<Canvas>
  <Story
    name="Default"
    args={{
      onPress: action('onPress'),
      label: 'Hello World',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Disabled"
    args={{
      onPress: action('onPress'),
      label: 'Disabled',
      disabled: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
