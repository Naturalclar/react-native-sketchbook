import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs';
import {action} from '@storybook/addon-actions';
import {PrimaryButton} from './PrimaryButton';
import {Typography} from '../Typography';

<Meta title={'Buttons/PrimaryButton'} component={PrimaryButton} />

# PrimaryButton

Example of Button Icon extended from Base Button

## Props

<ArgsTable of={PrimaryButton} />

export const Template = (args) => <PrimaryButton {...args} />;

<Canvas>
  <Story
    name="Default"
    args={{
      onPress: action('onPress'),
      label: 'Hello World',
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Disabled"
    args={{
      onPress: action('onPress'),
      label: 'Disabled',
      disabled: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
