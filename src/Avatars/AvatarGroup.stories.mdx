import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs';
import {Avatar} from './Avatar';
import {AvatarGroup} from './AvatarGroup';

<Meta title={'Avatars/AvatarGroup'} component={AvatarGroup} />

# AvatarGroup

Component that displays multiple Avatars

## Props

<ArgsTable of={AvatarGroup} />

export const Template = (args) => {
  const avatars = [...new Array(8)];
  return (
    <AvatarGroup {...args}>
      {avatars.map((avatar) => (
        <Avatar source={{uri: 'https://www.github.com/Naturalclar.png'}} />
      ))}
    </AvatarGroup>
  );
};

<Canvas>
  <Story
    name="Default"
    args={{
      source: {
        size: 30,
        uri: 'https://www.github.com/Naturalclar.png',
      },
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
