import {DefaultColorTheme} from './colors';
import {DefaultFontSize, DefaultSpaceSize} from './size';

export const DefaultTheme = {
  colors: DefaultColorTheme,
  fontSizes: DefaultFontSize,
  spaceSizes: DefaultSpaceSize,
} as const;
